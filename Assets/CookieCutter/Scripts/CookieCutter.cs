using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CookieCutter : Draggable
{
    // Start is called before the first frame update
    public Texture2D cookieShape;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
